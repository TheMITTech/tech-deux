---
import StickyNavbar from "../components/sticky-navbar.astro";
import AboutNavBar from "../components/about-nav-bar.astro";
import Base from "./base.astro";
import Footer from "../components/footer.astro";

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<Base>
  <StickyNavbar />
  <main class="about-page">
    <div class="about-page__grid">
      <aside class="about-page__sidebar">
        <AboutNavBar />
      </aside>
      <article class="about-page__content">
        <h1>{title}</h1>
        <slot />
      </article>
    </div>
  </main>
  <Footer />
</Base>

<style>
  .about-page {
    max-width: var(--width-wide);
    margin-inline: auto;
    padding: var(--space-12) var(--space-6);
  }

  .about-page__grid {
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: var(--space-12);
    align-items: start;
  }

  .about-page__sidebar {
    position: sticky;
    top: calc(var(--space-12));
  }

  .about-page__content {
    font-family: var(--font-serif);
    color: var(--color-text);
    font-size: var(--text-md);
    line-height: var(--leading-body);
    max-width: var(--width-content);
  }

  .about-page__content :global(h1) {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-2xl);
    line-height: var(--leading-tight);
    margin-bottom: var(--space-6);
    color: var(--color-text);
  }

  .about-page__content :global(h2) {
    font-family: var(--font-display);
    font-weight: 700;
    font-size: var(--text-lg);
    line-height: var(--leading-tight);
    margin-top: var(--space-8);
    margin-bottom: var(--space-3);
  }

  .about-page__content :global(p) {
    margin-bottom: var(--space-4);
  }

  .about-page__content :global(a) {
    color: var(--color-link);
    text-decoration: underline;
  }

  .about-page__content :global(a:hover) {
    color: var(--color-link-hover);
  }

  @media (max-width: 720px) {
    .about-page__grid {
      grid-template-columns: 1fr;
      gap: var(--space-8);
    }

    .about-page__sidebar {
      position: static;
    }
  }
</style>
